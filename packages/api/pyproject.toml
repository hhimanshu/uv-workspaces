[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "httpx",
    "pytest"
]

[tool.poetry.scripts]
start-server = "uvicorn main:app --reload"
test = "pytest"
